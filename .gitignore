node_modules/
dist/
coverage/
*.tsbuildinfo
.tmp/

# Rendered graphic output; the committed source rebuilds it. The README GIF is the
# exception: GitHub plays it inline, so it has to be in the repo.
graphic/out/*
!graphic/out/architecture.gif

.DS_Store
*.log

# Nothing in this repo reads a .env, and nothing should start.
.env
.env.*
